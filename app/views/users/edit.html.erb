<br>
<ul>
<%=display_error_messages_if_any(@user)%>
</ul>
<br>

<%= form_with(model: @user) do |f| %>
        <%= render partial: "form1", locals: {f: f} %>
        <%= created_via_omni?(@user, f)%>
        <%= f.submit :update%>
    <% end %>